{#
  {% include '_partials/contentBuilder' with {contentField: entry.mainContent, blocksDir: 'mainContent'} %}
#}
{% set blocksDir = blocksDir|default('mainContent') %}
{% if contentField is defined and contentField is not empty %}
  {% for matrixBlock in contentField.all %}
    {% set include_type = "_partials/blocks/#{blocksDir}/#{matrixBlock.type}" %}
    {% include include_type with {'block': matrixBlock} %}
  {% endfor %}
{% endif %}
