{% if entry.status == 'live' %}
  <div class="btn-wrap {{ entry.alignmentButton }}">
    <a class="btn" href="{{ entry.buttonLink }}" {{ entry.openInNewWindow ? 'target="_blank"' }}>{{ entry.caption }}</a>
  </div>
{% endif %}
